---
import BaseLayout from '../layouts/BaseLayout.astro';
import index from '../../entities/index.json';
---

<BaseLayout title="NFTK – Baza Wiedzy">
  <h2>Wszystkie encje</h2>
  <ul>
    {Object.values(index).map((entity) => (
      <li>
        <a href={`/${entity.slug}`}>{entity.name} ({entity.type})</a>
      </li>
    ))}
  </ul>
</BaseLayout>
