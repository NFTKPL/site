---
import index from '../../entities/index.json';
---

<!DOCTYPE html>
<html lang="pl">
  <head>
    <meta charset="utf-8" />
    <title>NFTK – Nowoczesna Faktograficzna Teczka Wiedzy</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body>
    <main style="padding: 2rem; font-family: sans-serif">
      <h1>NFTK – Baza Wiedzy o Osobach i Wydarzeniach</h1>
      <ul>
        {Object.values(index).map((entity) => (
          <li>
            <a href={`/${entity.slug}`}>
              {entity.name} ({entity.type})
            </a>
          </li>
        ))}
      </ul>
    </main>
  </body>
</html>
